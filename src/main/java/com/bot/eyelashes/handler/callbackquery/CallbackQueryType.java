package com.bot.eyelashes.handler.callbackquery;

import com.bot.eyelashes.handler.Handle;

public enum CallbackQueryType {
    REGISTRATION(new ), AUTH, MENU, INFO, START,MENU_CLIENT, MENU_MASTER;
    private final Handle handle;

    CallbackQueryType() {
    }

    public static
}
